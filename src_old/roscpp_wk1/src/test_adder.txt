#  빌드 & 환경
cd ~/ws && catkin_make --pkg roscpp_wk1
source devel/setup.bash

#  마스터
roscore

#  서버
rosrun roscpp_wk1 adder_server

# 클라이언트(둘 중 택1)
rosrun roscpp_wk1 adder_client
# 또는 도구로 바로 호출
rosservice call /add_two_ints "a: 10 b: 32"
